# CoW Protocol Settlement Interface

Shared types and instruction builders for the CoW Protocol settlement program.

Used by on-chain programs (to parse instruction bytes) and by off-chain code (to build them). Contains only the protocol's wire format, that is, what bytes go on the chain.
