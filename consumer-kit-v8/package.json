{
  "name": "consumer-kit-v8",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Consumer that picks the @solana/kit version the client is ACTUALLY built against (v8). Everything works at runtime — but the client's declared peer range rejects it, so the install is blocked.",
  "dependencies": {
    "cow-solana-settlement-client": "file:../programs/settlement/idl/client/js",
    "@solana/kit": "^8.0.0"
  },
  "scripts": {
    "build": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.7.0"
  }
}
